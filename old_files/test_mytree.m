clc
clear
close all

% Initialize the tree
tr = mytree();

% Create a node
new_node = [1, 1, 0, 0];

% Add the node to the tree
tr.add_node(new_node);

% Print tree nodes array
% tr.print_nodes();



